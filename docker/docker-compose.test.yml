services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: order_inventory_test_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"

